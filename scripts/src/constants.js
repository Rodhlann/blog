const ROOT_PATH = '.'

const INPUT_PATH = `${ROOT_PATH}/text_posts`

const POSTS_PATH = `${ROOT_PATH}/posts`
const CODE_PATH = `${POSTS_PATH}/code`
const BREAD_PATH = `${POSTS_PATH}/bread`

const HOME_URL = 'https://timpepper.dev'
const BLOG_URL = `${HOME_URL}/blog`

module.exports = {
  ROOT_PATH,
  INPUT_PATH,
  POSTS_PATH,
  CODE_PATH,
  BREAD_PATH,
  HOME_URL,
  BLOG_URL
}